type BookEntity{
    id:ID
    name:String
    cost:Int
    author:String
}

type Query{
    getAllBooks: [BookEntity]
}

type Mutation{
    saveBook(name:String!, cost:Int!, author:String!): BookEntity
}
